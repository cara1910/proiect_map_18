name: CI/CD Pipeline for Binary Tree Performance with NGINX

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v3

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2

    - name: Log in to DockerHub
      uses: docker/login-action@v2
      with:
        username: caracook1e
        password: PAROLAdocker1

    - name: Build and Push Docker Image
      uses: docker/build-push-action@v4
      with:
        context: .
        push: true
        tags: caracook1e/binary-tree-performance-nginx:latest

    - name: Deploy to Local Server (Optional)
      run: |
        echo "Deploy steps here if necessary"
